import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

# 组件

MDX的一大优点是，您可以编写标记并指定要使用的组件，而不是HTML元素。
下表列出了那些可以覆盖的HTML元素、所需的标记以及正常的JSX等效元素。
但是，以`a`的第一行为例，您可以通过以下操作将该超链接替换为`<FancyLink>`:

```jsx
import Readme from './readme.md' // Assumes an integration is used to compile MDX -> JS.
import { FancyLink } from './components/fancy-link.js'
;<Readme components={{ a: FancyLink }} />
```

更多关于如何使用markdown的信息可以在[CommonMark][]中找到。

## `a`

<Tabs>
<TabItem value="mdx" label="Markdown语法">

```mdx chrome=no
[MDX](https://mdxjs.com 'title')
```

</TabItem>
<TabItem value="jsx" label="等效JSX">

```jsx chrome=no
<>
  <p>
    <a href="https://mdxjs.com" title="title">
      MDX
    </a>
  </p>
</>
```

</TabItem>
</Tabs>

## `blockquote`

<table>
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Markdown syntax</th>
      <th scope="col">Equivalent JSX</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">`blockquote`</th>
      <td>```mdx chrome=no > A greater than… ```</td>
      <td>
        ```jsx chrome=no
        <>
          <blockquote>
            <p>A greater than…</p>
          </blockquote>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`br`</th>
      <td>```mdx chrome=no A backslash\ before a line break… ```</td>
      <td>
        ```jsx chrome=no
        <>
          <p>
            A backslash
            <br />
            before a line break…
          </p>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`code`</th>
      <td>
        ```mdx chrome=no Some `backticks` for inline. ```js backtick.fences('for
        blocks') ```
      </td>
      <td>
        ```jsx chrome=no
        <>
          <p>
            Some <code>backticks</code> for inline.
          </p>
          <pre>
            <code className="language-javascript">
              backtick.fences('for blocks')
            </code>
          </pre>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`em`</th>
      <td>```mdx chrome=no Some *asterisks* for emphasis. ```</td>
      <td>
        ```jsx chrome=no
        <>
          <p>
            Some <em>asterisks</em> for emphasis.
          </p>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`h1`</th>
      <td>```mdx chrome=no # One number sign… ```</td>
      <td>
        ```jsx chrome=no
        <>
          <h1>One number sign…</h1>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`h2`</th>
      <td>```mdx chrome=no ## Two number signs… ```</td>
      <td>
        ```jsx chrome=no
        <>
          <h2>Two number signs…</h2>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`h3`</th>
      <td>```mdx chrome=no ### Three number signs… ```</td>
      <td>
        ```jsx chrome=no
        <>
          <h3>Three number signs…</h3>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`h4`</th>
      <td>```mdx chrome=no #### Four number signs… ```</td>
      <td>
        ```jsx chrome=no
        <>
          <h4>Four number signs…</h4>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`h5`</th>
      <td>```mdx chrome=no ##### Five number signs… ```</td>
      <td>
        ```jsx chrome=no
        <>
          <h5>Five number signs…</h5>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`h6`</th>
      <td>```mdx chrome=no ###### Six number signs… ```</td>
      <td>
        ```jsx chrome=no
        <>
          <h6>Six number signs…</h6>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`hr`</th>
      <td>```mdx chrome=no Three asterisks for a thematic break: *** ```</td>
      <td>
        ```jsx chrome=no
        <>
          <p>Three asterisks for a thematic break:</p>
          <hr />
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`img`</th>
      <td>```mdx chrome=no ![Alt text](/logo.png "title") ```</td>
      <td>
        ```jsx chrome=no
        <>
          <p>
            <img src="/logo.png" alt="Alt text" title="title" />
          </p>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`li`</th>
      <td>
        ```mdx chrome=no * asterisks for unordered items 1. decimals and a dot
        for ordered items ```
      </td>
      <td>
        ```jsx chrome=no
        <>
          <ul>
            <li>asterisks for unordered items</li>
          </ul>
          <ol>
            <li>decimals and a dot for ordered items</li>
          </ol>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`ol`</th>
      <td>```mdx chrome=no 2. decimals and a dot for ordered ```</td>
      <td>
        ```jsx chrome=no
        <>
          <ol>
            <li>decimals and a dot for ordered</li>
          </ol>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`p`</th>
      <td>```mdx chrome=no Just some text… ```</td>
      <td>
        ```jsx chrome=no
        <>
          <p>Just some text…</p>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`pre`</th>
      <td>````mdx chrome=no ```js backtick.fences('for blocks') ``` ````</td>
      <td>
        ```jsx chrome=no
        <>
          <pre>
            <code className="language-javascript">
              backtick.fences('for blocks')
            </code>
          </pre>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`strong`</th>
      <td>```mdx chrome=no Two **asterisks** for strong. ```</td>
      <td>
        ```jsx chrome=no
        <>
          <p>
            Two <strong>asterisks</strong> for strong.
          </p>
        </>
        ```
      </td>
    </tr>
    <tr>
      <th scope="row">`ul`</th>
      <td>```mdx chrome=no * asterisks for unordered ```</td>
      <td>
        ```jsx chrome=no
        <>
          <ul>
            <li>asterisks for unordered</li>
          </ul>
        </>
        ```
      </td>
    </tr>
  </tbody>
</table>

可以覆盖的组件使用它们的标准HTML名称。
通常，在markdown里，这些名字是: `a`, `blockquote`, `br`, `code`, `em`,
`h1`, `h2`, `h3`, `h4`, `h5`, `h6`, `hr`, `img`, `li`, `ol`, `p`, `pre`,
`strong`, 和 `ul`.  
与 [`remark-gfm`][gfm] ([查看指南][guide-gfm]), 你也可以用: `del`,`input`, `section`, `sup`, `table`, `tbody`, `td`, `th`, `thead`, 和 `tr`.  
其他添加了对新结构的支持并宣称它们可以与rehype一起工作的remark插件也可以与MDX一起工作。

更多关于组件的信息，请参见[¶components in§Using MDX][components]。

[gfm]: https://github.com/remarkjs/remark-gfm
[components]: ./docs/using-mdx/#components
[commonmark]: https://spec.commonmark.org/current/
[guide-gfm]: ./guides/gfm/
