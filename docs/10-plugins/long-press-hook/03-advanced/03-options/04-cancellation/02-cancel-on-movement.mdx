# 移动时取消

如果长按，移出边界时应取消
## 名字

cancelOnMovement

## 类型

boolean | number

## 默认值

false

## 描述

如果长按应该取消当鼠标/触摸/指针移动到足够远的初始按位置。

可能的值:

- false - 禁用移动取消
- true - 启用取消移动和使用默认的25px边界大小
- number - 设置一个特定的边界值(以像素为单位)(在其中移动不会取消长按的正方形边大小)
​
