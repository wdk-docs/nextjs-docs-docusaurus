---
title: draftMode
description: draftMode函数的API参考。
---

`draftMode`函数允许你在[服务器组件](/docs/getting-started/react-essentials)中检测[草稿模式](/docs/app/building-your-application/configuring/draft-mode)。

```jsx title="app/page.js"
import { draftMode } from 'next/headers'

export default function Page() {
  const { isEnabled } = draftMode()
  return (
    <main>
      <h1>My Blog Post</h1>
      <p>Draft Mode is currently {isEnabled ? 'Enabled' : 'Disabled'}</p>
    </main>
  )
}
```

## 版本历史

| 版本      | 变化                    |
| --------- | ----------------------- |
| `v13.4.0` | `draftMode` introduced. |
